# secrets & local data
.env
.env.*
!.env.example
data/
uploads/

# generated / redundant artifacts
starter-datasets.zip
samples/*.tmp
evaluation/reports/

# provided material — kept locally for smoke testing, not part of the source repo
# (the starter-datasets/**/README.md files stay tracked as dataset provenance)
superjoin-vit-2026-assignment.pdf
starter-datasets/*.pdf
starter-datasets/**/*.pdf

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# node (only used ad-hoc for a headless jsdom UI check — never a repo dependency)
node_modules/
package-lock.json

# tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# sqlite runtime files
*.db
*.db-wal
*.db-shm
*.db-journal

# IDE / editor
.idea/
.vscode/
*.swp
.DS_Store
