# Runtime + dev dependencies, grouped by the phase that first needed them.
# Deliberately small: no vector DB, no ORM, no embedding model, no OCR libs
# (all considered and deferred — see docs/DECISIONS.md and docs/RISKS.md).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
httpx>=0.27

# Phase 2 — PDF ingestion
pymupdf>=1.24

# Phase 4 — LLM candidate extraction (Google Gemini; imported lazily inside
# app/llm.py so the app and the test suite load without it)
google-genai>=1.0

# Phase 5 — evidence verification (conservative fuzzy matching only)
rapidfuzz>=3.6

# Phase 11 — API (multipart file upload for POST /documents)
python-multipart>=0.0.9

# dev / test
pytest>=8.0
ruff>=0.6
httpx2>=2.12          # starlette>=1.6 TestClient backend (silences the httpx deprecation notice)
