"""Phase 10 — evaluation harness.

Runs the pipeline (or evaluates an existing database) and checks **expected
properties**, never hard-coded answers, plus a curated config sweep. See
docs/EVALUATION_PLAN.md.
"""
